{
  "name": "business-website-skill-site",
  "private": true,
  "version": "1.4.2",
  "scripts": {
    "build:site": "rm -rf public && mkdir -p public && cp -R site/. public/",
    "validate:site": "python3 scripts/audit_static_site.py site index.html --strict-seo --no-placeholders",
    "package:runtime": "python3 scripts/package_runtime_skill.py"
  }
}
